pipeline {
    agent any

    stages {
        stage('docker Build') {
            steps {
                sh """
                    cd src/frontend
                    podman system migrate
                    docker pull python
                """
            }
        }
    }
}
