pipeline {
    agent any

    stages {
        stage('docker Build') {
            steps {
                sh """
                    cd src/frontend
                    docker build -t frontend .
                """
            }
        }
  
    
      stage('SonarQube analysis') {
          steps{
              script{
                def scannerHome = tool 'frontend-scanner';
                withSonarQubeEnv('frontend-scanner') { // If you have configured more than one global server connection, you can specify its name
                  sh "${scannerHome}/bin/sonar-scanner"
                }
              }
          }
      }
    }
}
