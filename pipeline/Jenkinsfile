pipeline {
    agent any

    stages {
        stage('docker Build') {
            steps {
                sh """
                    cd src/frontend
                    su root
                    docker build .
                """
            }
        }
    }
}
